import React from "react";
import { useAuth } from "../App.jsx";
import Layout from "../components/Layout.jsx";

function Step({ number, title, children }) {
  return (
    <div style={{ display: "flex", gap: 14, marginBottom: 18 }}>
      <div
        style={{
          width: 28,
          height: 28,
          borderRadius: "50%",
          background: "#eef2ff",
          color: "#1d4ed8",
          display: "flex",
          alignItems: "center",
          justifyContent: "center",
          fontWeight: 700,
          fontSize: 14,
          flexShrink: 0,
        }}
      >
        {number}
      </div>
      <div>
        <div style={{ fontWeight: 600, marginBottom: 2 }}>{title}</div>
        <div style={{ color: "#555", fontSize: 14, lineHeight: 1.6 }}>{children}</div>
      </div>
    </div>
  );
}

function UserManual() {
  return (
    <>
      <div className="card">
        <h3 style={{ marginTop: 0 }}>เข้าสู่ระบบครั้งแรก</h3>
        <Step number="1" title="เข้าสู่ระบบด้วย SSO มหาวิทยาลัย">
          ล็อกอินครั้งแรกต้องใช้บัญชี SSO ของมหาวิทยาลัยเท่านั้น กดปุ่ม "เข้าสู่ระบบด้วย SSO มหาวิทยาลัย" ที่หน้าแรก แล้วล็อกอินตามปกติ
        </Step>
        <Step number="2" title="ตั้งรหัสผ่าน (ไม่บังคับ)">
          หลังล็อกอินสำเร็จครั้งแรก จะมีหน้าต่างเด้งขึ้นมาให้ตั้งรหัสผ่าน ถ้าตั้งไว้ ครั้งถัดไปจะล็อกอินด้วย username/password แทนได้ ไม่ต้องผ่าน SSO ทุกครั้งก็ได้ ถ้าไม่ต้องการตั้งตอนนี้ กด "ข้ามไปก่อน" ได้ (จะไม่มีป็อปอัปนี้อีกจนกว่าจะตั้งจริง)
        </Step>
        <Step number="3" title="สิทธิ์การใช้งานห้อง">
          ห้องที่ใช้งานได้ขึ้นอยู่กับสิทธิ์ที่ admin กำหนดไว้ บางห้องอาจได้สิทธิ์อัตโนมัติตามคณะ/สาขา บางห้องต้องรอ admin อนุมัติ ถ้าไม่มีสิทธิ์ห้องไหน ปุ่มของห้องนั้นจะกดไม่ได้
        </Step>
      </div>

      <div className="card">
        <h3 style={{ marginTop: 0 }}>วิธีปลดล็อกประตู (สำคัญ: ต้องมี 2 ขั้นตอน)</h3>
        <Step number="1" title="กดปุ่ม “ขอสิทธิ์” ที่เว็บ">
          กดปุ่มขอสิทธิ์ของห้องที่ต้องการจากหน้าเว็บ (หรือสแกน QR Code ที่ติดหน้าห้องเพื่อเปิดหน้าห้องนั้นโดยตรง)
        </Step>
        <Step number="2" title="ระบบจะขึ้นข้อความว่า “กรุณากดปุ่มที่หน้าห้อง”">
          นี่ไม่ใช่การปลดล็อกจริง แค่เป็นการขอสิทธิ์ชั่วคราวเท่านั้น เพื่อความปลอดภัยที่ไม่ให้ใครสั่งเปิดประตูจากที่ไหนก็ได้
        </Step>
        <Step number="3" title="กดปุ่มจริงที่ติดอยู่หน้าห้อง ภายใน 10 วินาที">
          ต้องมีคนไปกดปุ่มที่หน้าห้องจริงๆ ภายในเวลาที่กำหนด ประตูถึงจะปลดล็อกจริง (ค้างไว้ 5 วินาทีแล้วล็อกกลับอัตโนมัติ) ถ้าไม่มีใครกดภายในเวลา คำขอจะถูกยกเลิกไปเอง ต้องขอสิทธิ์ใหม่อีกครั้ง
        </Step>
      </div>

      <div className="card">
        <h3 style={{ marginTop: 0 }}>สแกน QR Code หน้าห้อง</h3>
        <p style={{ color: "#555", fontSize: 14, lineHeight: 1.6 }}>
          แต่ละห้องมี QR Code ติดไว้หน้าห้อง สแกนแล้วจะพาไปที่หน้าขอสิทธิ์ของห้องนั้นโดยตรง (ถ้ายังไม่ได้ล็อกอิน ระบบจะให้ล็อกอินก่อน แล้วพากลับมาที่ห้องเดิมให้อัตโนมัติหลังล็อกอินสำเร็จ)
        </p>
      </div>

      <div className="card">
        <h3 style={{ marginTop: 0 }}>ถ้าห้องไม่มีอินเทอร์เน็ต (ฉุกเฉิน)</h3>
        <p style={{ color: "#555", fontSize: 14, lineHeight: 1.6, marginBottom: 12 }}>
          ถ้าหน้าจอที่ห้องขึ้นสถานะ "ออฟไลน์" ให้ทำตามนี้:
        </p>
        <Step number="1" title="กดปุ่มที่หน้าห้อง 1 ครั้ง">
          อุปกรณ์จะเปิดเป็นจุดปล่อยสัญญาณ WiFi ชื่อ "DoorLock-Offline"
        </Step>
        <Step number="2" title="เชื่อมต่อ WiFi นั้นด้วยมือถือ">
          จะมีหน้าเว็บเด้งขึ้นมาให้กรอกรหัสฉุกเฉิน 6 หลัก (ขอรหัสได้จาก admin)
        </Step>
        <Step number="3" title="กรอกรหัสแล้วกดปลดล็อก">
          รหัสแต่ละชุดใช้ได้แค่ครั้งเดียว ถ้าใช้ไปแล้วต้องขอรหัสชุดใหม่จาก admin
        </Step>
      </div>
      <div className="card">
        <h3 style={{ marginTop: 0 }}>ถ้าอุปกรณ์หน้าห้องทำงานผิดปกติ</h3>
        <p style={{ color: "#555", fontSize: 14, lineHeight: 1.6 }}>
          หากจอที่หน้าห้องค้าง ไม่ตอบสนอง หรือทำงานผิดพลาด สามารถ<b>กดปุ่มที่หน้าห้องค้างไว้ 5 วินาที</b> จอจะขึ้นข้อความ "Resetting..." แล้วอุปกรณ์จะรีสตาร์ทตัวเองใหม่ทั้งหมด (เหมือนปิด-เปิดเครื่องใหม่) ใช้เวลาประมาณ 10-15 วินาทีก่อนกลับมาใช้งานได้ตามปกติ
        </p>
      </div>
    </>
  );
}

function AdminManual() {
  return (
    <>
      <div className="card">
        <h3 style={{ marginTop: 0 }}>จัดการห้อง</h3>
        <Step number="1" title="เพิ่มห้องใหม่">
          ไปที่เมนู "จัดการห้อง" พิมพ์ชื่อห้องในช่องด้านบนสุดแล้วกด "เพิ่มห้อง" ระบบจะสุ่ม secret ให้อัตโนมัติ ต้องคัดลอกไปใส่ในโค้ด ESP32 ของห้องนั้น (ตัวแปร <code>WS_PATH</code>)
        </Step>
        <Step number="2" title="QR Code">
          กดปุ่ม "QR Code" ที่การ์ดของห้องนั้น จะได้ QR สำหรับพิมพ์ติดหน้าห้อง สแกนแล้วพา user ไปที่หน้าขอสิทธิ์ของห้องนั้นโดยตรง
        </Step>
        <Step number="3" title="ให้สิทธิ์อัตโนมัติตามคณะ/สาขา">
          พิมพ์ชื่อคณะหรือสาขา (คั่นด้วยจุลภาคถ้ามีหลายค่า) ในช่อง "ให้สิทธิ์อัตโนมัติ" ของห้องนั้น user คนไหนที่คณะ/สาขาตรงกับที่ระบุไว้ จะได้สิทธิ์ปลดล็อกห้องนี้ทันทีตอนล็อกอิน SSO ครั้งแรก ไม่ต้องรออนุมัติ
        </Step>
        <Step number="4" title="รหัสฉุกเฉิน (offline)">
          กด "สร้างชุดใหม่ทั้ง 10 ชุด" เพื่อสุ่มรหัส 6 หลักสำหรับใช้ตอน ESP32 ไม่มีอินเทอร์เน็ต ต้องให้ ESP32 ของห้องนั้นออนไลน์อย่างน้อยครั้งหนึ่งเพื่อรับซิงก์ชุดรหัส รหัสที่ใช้ไปแล้วจะขึ้นสถานะ "ใช้แล้ว" อัตโนมัติ
        </Step>
      </div>

      <div className="card">
        <h3 style={{ marginTop: 0 }}>จัดการผู้ใช้</h3>
        <Step number="1" title="ค้นหา">
          ค้นหา user จากชื่อ, username หรือรหัสนักศึกษาได้จากช่องค้นหาด้านบน
        </Step>
        <Step number="2" title="ดูรายละเอียด">
          คลิกที่ชื่อ user เพื่อดูข้อมูลเพิ่มเติม เช่น อีเมล รหัสนักศึกษา คณะ สาขา
        </Step>
        <Step number="3" title="กำหนดสิทธิ์ห้อง">
          ติ๊ก/ไม่ติ๊กในตารางเพื่อเปิด-ปิดสิทธิ์การปลดล็อกแต่ละห้องของ user คนนั้นได้โดยตรง
        </Step>
        <Step number="4" title="เปลี่ยนบทบาท">
          คลิกที่ badge บทบาท (user/admin) เพื่อสลับสิทธิ์การเป็นแอดมิน — admin จะปลดล็อกได้ทุกห้องโดยอัตโนมัติ
        </Step>
      </div>

      <div className="card">
        <h3 style={{ marginTop: 0 }}>ประวัติการใช้งาน</h3>
        <p style={{ color: "#555", fontSize: 14, lineHeight: 1.6 }}>
          ไปที่เมนู "ประวัติการใช้งาน" เพื่อดูบันทึกการปลดล็อกย้อนหลัง กรองได้ตามวันที่ ช่วงเวลา ห้อง หรือชื่อผู้ใช้ ใช้ตรวจสอบได้ว่าใครขอสิทธิ์ห้องไหนตอนไหน และสำเร็จหรือไม่ (เช่น หมดเวลาไม่มีคนกดปุ่มที่หน้าห้อง)
        </p>
      </div>

      <div className="card">
        <h3 style={{ marginTop: 0 }}>ระบบความปลอดภัย 2 ขั้นตอน</h3>
        <p style={{ color: "#555", fontSize: 14, lineHeight: 1.6 }}>
          ปุ่มบนเว็บเป็นแค่การ "ขอสิทธิ์" เท่านั้น ประตูจะไม่ปลดล็อกจนกว่าจะมีคนไปกดปุ่มจริงที่ติดตั้งไว้หน้าห้องภายใน 10 วินาที เพื่อป้องกันการสั่งปลดล็อกจากระยะไกลโดยไม่มีคนอยู่หน้าห้องจริง
        </p>
      </div>
      <div className="card">
        <h3 style={{ marginTop: 0 }}>การรีเซ็ตอุปกรณ์หน้าห้อง</h3>
        <p style={{ color: "#555", fontSize: 14, lineHeight: 1.6 }}>
          ปุ่มที่ติดตั้งไว้หน้าห้องแต่ละใบมีหลายหน้าที่ขึ้นอยู่กับจังหวะการกด: กดสั้นใช้ยืนยันปลดล็อก/เข้า-ออกโหมดฉุกเฉิน ส่วน<b>การกดค้างไว้ 5 วินาทีจะสั่งรีสตาร์ทอุปกรณ์ทันที</b> ใช้เมื่ออุปกรณ์ค้างหรือทำงานผิดปกติ แนะนำให้แจ้ง user ทราบวิธีนี้ไว้ล่วงหน้า เพื่อลดภาระการติดต่อ admin ในกรณีปัญหาเล็กน้อยที่แก้ได้เอง
        </p>
      </div>
    </>
  );
}

export default function Manual() {
  const { user } = useAuth();
  const isAdmin = user?.role === "admin";

  return (
    <Layout
      title={isAdmin ? "คู่มือการใช้งาน (Admin)" : "คู่มือการใช้งาน"}
      subtitle={isAdmin ? "วิธีจัดการห้อง ผู้ใช้ และตรวจสอบประวัติการใช้งาน" : "วิธีล็อกอินและปลดล็อกประตู"}
    >
      {isAdmin ? <AdminManual /> : <UserManual />}
    </Layout>
  );
}